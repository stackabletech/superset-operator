---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
metadata:
  name: install-test-container
timeout: 300
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: test-celery-worker
  labels:
    app: test-celery-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-celery-worker
  template:
    metadata:
      labels:
        app: test-celery-worker
    spec:
      containers:
        - name: test-celery-worker
          image: oci.stackable.tech/sdp/testing-tools:0.3.0-stackable0.0.0-dev
          stdin: true
          tty: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "512m"
            limits:
              memory: "128Mi"
              cpu: "1"
