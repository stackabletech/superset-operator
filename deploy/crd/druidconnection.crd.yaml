---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: druidconnections.command.superset.stackable.tech
spec:
  group: command.superset.stackable.tech
  names:
    categories: []
    kind: DruidConnection
    plural: druidconnections
    shortNames: []
    singular: druidconnection
  scope: Namespaced
  versions:
    - additionalPrinterColumns: []
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: "Auto-generated derived type for DruidConnectionSpec via `CustomResource`"
          properties:
            spec:
              properties:
                druidClusterName:
                  type: string
                druidClusterNamespace:
                  type: string
                supersetClusterName:
                  type: string
                supersetClusterNamespace:
                  type: string
              required:
                - druidClusterName
                - druidClusterNamespace
                - supersetClusterName
                - supersetClusterNamespace
              type: object
            status:
              nullable: true
              properties:
                condition:
                  enum:
                    - Provisioned
                    - Importing
                    - Ready
                    - Failed
                  type: string
                startedAt:
                  description: Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
                  format: date-time
                  nullable: true
                  type: string
              required:
                - condition
              type: object
          required:
            - spec
          title: DruidConnection
          type: object
      served: true
      storage: true
      subresources:
        status: {}
